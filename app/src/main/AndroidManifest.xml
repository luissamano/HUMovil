<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.leondev.humovil">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appico"
        android:label="@string/app_name"
        android:roundIcon="@drawable/appico"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activitys.ActivityOem" />
        <activity android:name=".Activitys.ActivityCons" />
        <activity android:name=".Activitys.ActivityPlanta" />
        <activity android:name=".Activitys.ActivityProg" />
        <activity android:name=".Activitys.ActivityWOA" />
        <activity android:name=".Activitys.ActivityPicker"></activity>
    </application>

</manifest>